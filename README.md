# Марионетка.

## Разворачивание и запуск
```bash
# Install UV
python -m pip install uv

# Create uv.lock file
uv lock

# Start bot.
docker compose up
```

## Dependencies
Python 3.14
uv 0.9.26
Docker >=V2
Docker Compose